export enum TicketStatus {
    APPROVED = "APPROVED",
    DENIED = "DENIED",
    PENDING = "PENDING"
}

export enum TicketStatusOption {
    APPROVED = "APPROVED",
    DENIED = "DENIED",
    PENDING = "PENDING",
    ALL = "ALL"
}